# credict_prediction_system
A machine learning project that predicts whether a credit card customer will default on their payment using the UCI Credit Card Default Dataset. The project includes EDA, preprocessing, feature engineering, model training, hyperparameter tuning, and evaluation using models like Logistic Regression, Decision Tree, Random Forest, and XGBoost.
